## Домашнее задание 8 (необязательное)

**Мягкий дедлайн:** 15.03.2026 23:59

**Жёсткий дедлайн:** 18.03.2026 23:59

**Ссылки для сдачи:**

HW8-241 https://classroom.github.com/a/q8zVmhM6

HW8-242 https://classroom.github.com/a/q7GIObHB

HW8-243 https://classroom.github.com/a/L9JiQb8j

### **Тема: Машинное обучение и NLP**

#### **Задание**
1. Решить задачи классификации и кластеризации на датасете, посвящённом ментальному здоровью;
2. Проанализировать ошибки модели и результаты в целом.

Как скачать и прочитать датасет (если вы работаете в колабе или у вас линукс/мак):
```
!curl -L -o sentiment-analysis-for-mental-health.zip https://www.kaggle.com/api/v1/datasets/download/suchintikasarkar/sentiment-analysis-for-mental-health
!unzip sentiment-analysis-for-mental-health.zip
```

```
import pandas as pd
data = pd.read_csv('Combined Data.csv', index_col=0)
data.head()
```

Если у вас не работает то, что приведено выше, то лучше пойти [на страницу датасета](https://www.kaggle.com/datasets/suchintikasarkar/sentiment-analysis-for-mental-health) и скачать его оттуда.

**Пункты**:
1. (1 балл) Сделать EDA (Exploratory Data Analysis, разведочный анализ данных).

- Посмотрите, как устроены данные и как распределены классы;
- Нарисуйте три облака слов для самых частотных классов. Постарайтесь сделать их информативными: например, исключите стоп-слова и слова, общие для этих трёх классов.

2. (1 балл) Подготовить данные для обучения моделей:

- Предобработать тексты. Подумайте, какая предобработка вам нужна в зависимости от планируемого метода векторизации; 
- Разбить данные на обучающую и тестовую выборки. При выборе размера теста учитывайте, что в обучающей выборке должно быть достаточно данных для кросс-валидации.

3. (5 баллов, по 1 за эксперимент и 1 за анализ) Провести эксперименты, обучив разные варианты моделей для решения задачи:
    
- Попробовать минимум 2 метода векторизации из тех, про которые мы говорили на семинарах (мешок слов, Tf-Idf, word2vec, BERT), желательно не выбирать два метода одной природы (Tf-Idf + word2vec - ок, мешок слов + TF-Idf - хуже);
- Попробовать минимум 2 алгоритма классификации (KNN, LogReg, деревья, можно поискать что-то еще из sklearn);
- Для каждого варианта подобрать лучший набор гиперпараметров с помощью перебора по сетке (*grid search*);
- Для каждого варианта оценить качество на тестовой выборке (*classification report*) и построить матрицу ошибок (*confusion matrix*);
- Проанализировать полученные лучшие гиперпараметры, ошибки моделей и метрики, опишите полученные результаты.

    Несколько важных замечаний для экспериментов:

    - Чтобы эксперимент был честным и его результаты было легко интерпретировать, не меняйте за раз больше одного параметра. Например, вы обучили вариант tf-idf + logreg. После этого сначала попробуйте поменять что-то одно (метод векторизации или метод классификации), сравните результаты и примите решение о следующем изменении. Так вы всегда будете знать, чем именно вызвано изменение метрик;
    - При анализе получившихся результатов старайтесь подкреплять любые ваши выводы конкретными числами или графиками. 

4. (1 балл) Объяснить **своими словами**, как работают выбранные векторизаторы и классификаторы и что означает каждая из метрик.

5. (2 балла) Кластеризуйте и визуализируйте данные с лучшим методом векторизации (скорее всего это будет word2vec или BERT):

- Применить к данным один линейный (KMeans, аггломеративная кластеризация) и один нелинейный (DBSCAN) метод кластеризации;
- Нарисовать данные на плоскости с помощью двух методов снижения размерности (можно взять SVD или PCA в качестве первого и t-SNE или UMAP в качестве второго);
- Проанализируйте полученные результаты и предположите, как увиденная структура данных повлияла на результаты классификации.

